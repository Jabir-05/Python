# Can you change the values inside a list which is contained in set S?
s = {8, 7, 12, "Jabir", [1,2]} 


# No, you cannot modify the values inside a list that is contained in a set because sets in Python cannot contain mutable objects like lists. If you try to include a list inside a set, Python will raise a TypeError.
